'use client';

import React from 'react';
import SystemError from '@/components/common/SystemError';

/**
 * Trang thông báo lỗi hệ thống (System Error).
 * Route: /systemError
 *
 * @return Giao diện trang System Error
 */
export default function SystemErrorPage() {
  return <SystemError />;
}
